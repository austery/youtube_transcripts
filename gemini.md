请作为我的Markdown文件优化助手，执行以下任务：

**针对我提供的每个Markdown文件：**

1.  **移除HTML元素：** 请彻底删除文件内容中所有的HTML标签和属性（例如：`<div>`, `<span class="...">`, `<p>`, `<button>`, `<div id="toc">` 以及内联样式等）。只保留纯文本内容和Markdown格式（如 `#` 标题, `-` 列表等）。
2.  **提取关键词：** 从清理后的文本内容中，提取2到5个最具代表性的**中文关键词**。这些关键词应能准确反映文章的核心主题或重要概念。
3.  **更新Front Matter的Tags：**
    * 在文件的`front matter`（YAML区域，由 `---` 包裹的部分）中，找到 `tags:` 字段。
    * 将步骤2中提取的2-5个关键词作为新的标签，添加到 `tags:` 列表中。
    * 如果 `tags:` 字段已经存在其他标签，请**保留原有标签**，并将新提取的关键词添加进去，确保标签之间用逗号分隔或符合YAML列表格式。
    * 如果 `tags:` 字段不存在，请在 `front matter` 中创建一个，并将关键词添加进去。
    * **确保修改后的`front matter` 语法正确无误。**

**请按照以下输入和输出格式进行操作：**

**输入：**
就在当前目录寻找md 文件

**输出：**
请save 在 output folder
